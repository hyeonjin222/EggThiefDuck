# 📜 Project Wings Implementation History

이 문서는 프로젝트의 현재 진행 상태와 구현 완료된 히스토리를 기록합니다. 세션 시작 시 AI는 이 내용을 바탕으로 문맥을 파악합니다.

---

## 🎯 현재 목표 (Current Goal)
- [x] **변이형 시너지 (Mutation Synergy) 시스템:** 주무기가 여러 효과를 중첩하여 점점 강력해지는 시스템 구축.
- [x] **5레벨 마일스톤 시스템:** 일반 레벨은 스탯, 5레벨 단위는 무기 선택으로 이원화.
- [x] **동적 투사체 변이:** 레이저, 화염방사기 등 형태 변화와 관통/폭발 시너지 구현.

---

## 🚧 진행 중 / 다음 작업 (To-Do)
1. **패시브 무기 액터 구현:** '회전 달걀', '유도 폭탄', '화염병' 등 패시브 무기들의 실제 작동 로직(C++) 및 액터 제작.
2. **다양한 업그레이드 에셋 생성:** 새로 정의된 `EUpgradeType`에 맞춰 다양한 데이터 에셋(기관총, 레이저, 화염방사기 등) 제작.
3. **밸런스 조정:** 무기 간 시너지 효과 및 레벨업 속도 밸런싱.

---

## ✅ 완료된 기록 (History Log)

### 2026-05-22 (Day-Based Spawning System Implementation)
- **일차별 스폰 시스템 (Day-Based Spawning System):**
  - **날짜 개념 도입:** `FWaveSetting` 구조체에 `StartDay` 필드를 추가하여 시간뿐만 아니라 날짜별로도 웨이브를 설정할 수 있도록 확장.
  - **총 상대 시간(Total Relative Hour) 연산:** 게임 시작 시점(1일차 19시)을 기준으로 현재 날짜와 시간을 합산한 절대적인 경과 시간을 계산하는 로직 구현.
  - **연속적인 스케줄링:** 날짜가 바뀌어도 `UpdateCurrentWaveSetting`이 총 경과 시간을 추적하여, 1일차 새벽에서 2일차 저녁으로 넘어가는 구간 등 모든 시간대에서 정확한 웨이브 설정을 찾아내도록 로직 고도화.
  - **유연한 확장성:** 이제 에디터에서 `StartDay: 1, StartHour: 19`, `StartDay: 2, StartHour: 19`와 같이 설정하는 것만으로 날짜별 난이도 조절 및 몹 구성 변경이 가능함.
- **아이템 시스템 개선:**
  - **소멸 시간 연장:** 드롭 아이템(`ADropItemBase`)의 자동 소멸 시간(`LifeTime`)을 10초에서 60초(1분)로 대폭 연장하여 플레이어가 아이템을 회수할 수 있는 충분한 시간을 확보.
- **시간 시스템 리팩토링 및 낮 시간 스킵 구현:**
  - **속도 단위 정상화:** 기존에 시간 단위로 연산되던 `TimeScale`을 분(Minute) 단위로 리팩토링하여 직관적인 조절이 가능하도록 수정 (기본값: 현실 1초당 게임 1분).
  - **낮 시간 고속 스킵:** 아침 8시(정비 시작)부터 저녁 6시(전투 준비) 사이의 구간에 **50배속** 가속 로직을 적용. 지루한 낮 시간을 시각적으로 빠르게 스킵하고 즉시 다음 밤 전투로 이어지도록 개선.
  - **가속 배율 변수화:** 에디터에서 `DayTimeSpeedMultiplier` 수치를 조절하여 스킵 속도를 자유롭게 변경 가능하도록 구현.
- **최종 게임 템포 및 페이즈 설정:** 
  - **밤(전투) 속도:** 현실 1초당 게임 4분 (1분당 4시간 흐름)으로 조정. 밤 전체(12시간)가 약 **3분** 동안 진행되도록 밸런싱.
  - **낮(스킵) 속도:** 현실 1초당 게임 1시간 (가속 배율 15.0)으로 유지.
  - **페이즈 타이밍 조정:** 적들이 도망을 멈추고 완전히 소멸하는 낮(Day) 시작 시간을 8시에서 **7시**로 1시간 앞당겨 게임 템포를 더욱 빠르게 개선.
- **체력 회복 시스템 및 아이템 추가:**
  - **Heal 기능 구현:** `ADuckCharacter`에 비율 기반 체력 회복 함수(`Heal`)를 추가하여 유연한 회복 로직 구축.
  - **체력 아이템(`AHealthItem`) 추가:** 획득 시 즉시 최대 체력의 **10%**를 회복시켜주는 새로운 드롭 아이템 클래스 구현.
- **탄약 시스템(과열 메커니즘) 대규모 개편:**
  - **밸런스 재조정:** 최대 탄약 100, 발당 소모 20, 초당 회복 20으로 설정하여 총 5발 사격 및 완충까지 5초가 소요되도록 밸런싱.
  - **회복 규칙 변경:** 기존 '비사격 시 회복'에서 **'사격 중에도 항상 회복'** 방식으로 변경하여 사격 유지력을 소폭 상향.
  - **완충 잠금(Recharge Lock):** 탄약을 모두 소진하여 과열 상태가 되면, 게이지가 **100% 완충될 때까지 사격이 원천 봉쇄**되는 페널티 시스템 구현.
  - **시각적 피드백 강화:** 과열 시 머리 위 탄약바의 색상이 **빨간색**으로 변하며, 완충 시 다시 원래의 **파란색(0012FFFF)**으로 돌아오도록 순수 C++로 구현.
- **탄약 게이지 머리 위 UI 통합 (순수 C++):**
  - **부유형 UI 확장:** 플레이어 머리 위 체력바(`WBP_DuckHealthBar`)에 탄약 게이지를 추가로 표시할 수 있도록 `UHealthBarWidget` 기능을 확장.
  - **순수 C++ 위젯 바인딩:** 블루프린트 이벤트 대신 `meta = (BindWidget)`을 사용하여 `ProgressBar_Health`와 `ProgressBar_Ammo`를 C++에 직접 바인딩하고, `SetPercent` 로직을 C++ 내부에서 즉각 처리하도록 리팩토링.
  - **실시간 데이터 바인딩:** `ADuckCharacter`의 `Tick`에서 `CombatComponent`의 현재 탄약 게이지 비율을 실시간으로 가져와 부유형 위젯에 전달하도록 개선. 이를 통해 플레이어가 조준 중에도 자신의 탄약 상태를 즉시 파악 가능.
- **업그레이드 리롤(Reroll) 시스템 구현:**
  - **재화 소모형 리롤:** 레벨업 시 20골드를 사용하여 제시된 3개의 업그레이드 선택지를 즉시 다시 뽑을 수 있는 기능 추가.
  - **로직 리팩토링:** `ADuckCharacter`의 선택지 생성 로직을 `GenerateUpgradeOptions`로 분리하여 레벨업과 리롤 시 모두 동일한 규칙(5레벨 마일스톤 등)이 적용되도록 개선.
  - **UI 동적 갱신:** `UUpgradeScreenWidget`에 리롤 버튼을 바인딩하고, 골드가 부족할 경우 버튼을 자동으로 비활성화하는 예외 처리 구현.
- **경제 및 성장 밸런스 조정:**
  - **경험치 획득량 상향:** 경험치 아이템(`AExpItem`) 획득 시 지급되는 경험치 양을 10에서 **20**으로 2배 상향 조정하여 플레이어의 성장 속도를 가속화.
  - **골드 획득량 하향:** 골드 아이템(`AGoldItem`) 획득 시 지급되는 골드 양을 10원에서 **1원**으로 하향 조정하여 게임의 경제 템포를 조절.

### 2026-05-22 (Seamless Intro System Implementation)
- **심리스 인트로(Seamless Intro) 시스템 구현:**
  - **지연된 시작 로직:** 게임 진입 시 즉시 시간이 흐르거나 HUD가 표시되지 않고, 플레이어가 특정 행동을 하기 전까지 '대기' 상태를 유지하도록 `ADuckGameMode` 수정.
  - **시간 정지 메커니즘:** `bIsGameStarted` 플래그를 도입하여, 게임 시작 전까지는 15:00(오후 3시)에서 시간이 멈춰 있도록 `UpdateTime` 로직 고도화.
  - **동적 HUD 초기화:** `ADuckPlayerController`의 HUD 생성 로직을 `InitializeHUD`로 분리하여, 인트로가 끝나는 시점에만 UI가 나타나도록 개선.
  - **트리거 기반 게임 시작 (`AStartGameTrigger`):** 플레이어가 맵 중앙 등 특정 지점에 도달하면 `StartGame()`을 호출하는 전용 트리거 액터 클래스 구현.
  - **환경 변화 연출:** 게임 시작 시 `"IntroFence"` 태그를 가진 모든 액터를 즉시 파괴하여, 갇혀있던 구역이 개방되는 연출을 C++로 자동화.
- **시간 및 전투 사이클 최적화:**
  - **전투 시작 지점 변경:** 밤(Battle) 시작 시간을 19:00에서 **18:00**로 변경하여, 날짜 변경 알림과 동시에 전투가 시작되도록 게임 루프를 직관적으로 개선.
  - **스폰 로직 정밀도 개선:** `EnemySpawner`의 시간 비교 방식을 '절대 선형 시간(`Day * 24 + Hour`)으로 리팩토링. 이를 통해 `StartHour`에 18.1과 같은 소수점 수치를 입력해도 오차 없이 완벽하게 작동하도록 수정.

### 2026-05-21 (Data-Driven Weapon System Implementation)
- **무기 데이터 기반 시스템 (Data-Driven Weapon System):**
  - **하드코딩 제거:** `UDuckCombatComponent.cpp`에 직접 입력되어 있던 기관총, 샷건, 저격총 등의 수치를 모두 제거.
  - **무기 배율 시스템 확장:** `EUpgradeType`에 `Weapon_Mult_*` (연사력, 데미지, 탄속, 사거리, 게이지, 이동 속도 등) 및 `Weapon_Base_SpreadAngle` 타입을 추가.
  - **에디터 제어권 확보:** 이제 모든 무기의 상세 밸런스를 데이터 에셋(`UUpgradeDataAsset`)의 `Effects` 배열에서 직접 조절 가능.
  - **이동 속도 밸런스 조정:** 기본 이동 속도를 600에서 400으로 하향 조정하여 업그레이드 체감이 기획 의도에 맞게(20% 증가 시 480) 작동하도록 수정.
  - **투사체 물리 충돌 개선:** 산탄총 발사 시 달걀끼리 부딪혀 튕겨나가는 문제를 해결하기 위해 투사체 간 충돌을 무시(`Ignore WorldDynamic`)하도록 설정.
  - **무기 시너지(곱셈 누적) 시스템:** 무기 배율(`Weapon_*Multiplier`) 계산 방식을 덮어쓰기에서 곱셈 누적(`*=`) 방식으로 변경. 기관총(공속)과 저격총(데미지)을 섞었을 때 두 특징이 모두 반영된 하이브리드 무기 탄생이 가능하도록 아키텍처 개선.
  - **발사 개수 및 탄퍼짐 누적:** `MultiShotCount`와 `WeaponSpreadAngleBase`를 합산 방식으로 변경하여, 여러 무기 변이가 중첩될수록 더욱 풍성하고 화려한 사격이 가능하도록 수정.
  - **유연한 시너지:** 동일한 배율 시스템을 사용하여 향후 추가될 새로운 무기(화염방사기, 레이저 등)도 별도의 코드 수정 없이 데이터 설정만으로 구현 가능하도록 아키텍처 개선.
- **UI 및 시각적 연출 개선:**
  - **데미지 텍스트 정수화:** 데미지 텍스트가 소수점(예: 20.45)까지 표시되던 현상을 수정. `int32` 기반으로 리팩토링하고 `Floor` 처리를 적용하여 깔끔한 정수 수치만 노출되도록 개선.
  - **적 이동 애니메이션(Squash & Stretch) 강화:** 적이 이동할 때 통통 튀는 연출을 개선하기 위해, 수직 속도에 따른 스케일 변화 계수를 3배 늘리고 착지 순간 강하게 납작해지도록 보정하여 젤리처럼 찰진 움직임을 구현.

### 2026-05-21 (Mob Spawning System & Time Logic Update)
- **전투 페이즈 시간 및 라이팅 조정:**
  - **밤 시작 시간 변경:** 밤(전투) 시작 시간을 00:00에서 저녁 19:00(7 PM)으로 변경.
  - **페이즈 전환 로직 고도화:** 밤 19:00부터 익일 새벽 06:00까지를 Night 페이즈로 인식하도록 `CheckPhaseTransition` 로직 수정.
  - **시작 시간 설정:** 게임 시작 시 첫날 저녁 19:00부터 시작되도록 초기값 조정.
- **적 베이스 스탯 노출 (`AEnemyBase`):**
  - `MaxHealth`, `AttackDamage`를 `Enemy|Stats` 카테고리로 통합하고 블루프린트 디테일 창에서 직접 수정 가능하도록 `EditAnywhere` 부여.
- **시간 기반 몹 스폰 매니저 구현 (`AEnemySpawner`):**
  - **시간대별 웨이브 스케줄링:** `FWaveSetting` 구조체를 통해 19시, 21시, 0시, 3시 등 특정 시간마다 스폰 주기, 개체 수, 최대 제한, 몹 구성을 다르게 설정 가능.
  - **구간별 난이도 점프:** 정해진 시간에 도달하면 즉시 새로운 난이도 세팅(최대 개체 수 증가 등)으로 전환되는 계단식 난이도 시스템 구축.
  - **도넛형 스폰 (Donut Spawning):** 플레이어 주변 일정 반경(1200~1800) 외곽에서만 적이 생성되어 화면 밖에서 다가오도록 로직 구현.
  - **가중치 기반 풀링:** 각 세팅마다 닭, 돼지, 사슴 등 여러 적의 등장 확률을 가중치(Weight)로 관리하여 동적으로 몹 구성을 섞어서 스폰 가능.
- **스탯 연산 공식 표준화 (Weapon Multiplier):**
  - **공식 도입:** `(기본값 * 무기별_배율) * (1.0 + 강화_보너스)` 공식을 모든 전투 스탯(공격력, 공속, 사거리, 투사체 속도, 크기 등)에 적용.
  - **효과:** 무기 획득 순서와 상관없이 항상 일정한 최종 스탯을 보장하며, 무기 고유의 개성(배율)과 플레이어의 성장(보너스 퍼센트)을 완벽히 분리.
- **절대치 -> 퍼센트 기반 업그레이드 개편:**
  - 최대 체력(Max Health)을 제외한 모든 스탯(이동 속도, 게이지 양/회복량 등)을 퍼센트 누적 방식(Bonus Variable)으로 변경하여 연산 안정성 확보.
  - 최대 체력 증가 시 현재 체력도 동일 수치만큼 회복되도록 개선.
- **무기별 밸런스 조정:**
  - `FireRate` 기본값 0.2초 -> 0.5초로 변경 (초반 템포 조절).
  - **기관총 (Machine Gun):** 공속 2배, 최대 탄약/회복량 2배, 데미지 배율 0.7로 조정하여 지속 화력형 무기로 설정.
  - **샷건 (Shotgun):** 산탄 4발, 공속 50% 감소, 데미지 배율 0.7 적용.
  - **저격총 (Sniper):** 투사체 속도 3배, 데미지 배율 2배, 공속 50% 감소 및 '기본 3마리 관통' 능력을 부여하여 일직선 소탕 특화.
- **관통 횟수 (Piercing Count) 시스템 도입:**
  - `Stat_PiercingCount` 업그레이드 추가. 발사체가 무한히 뚫지 않고 지정된 횟수(`MaxPiercingCount`)만큼만 적을 관통한 후 소멸하도록 로직 고도화.
- **발사체 물리 및 충돌(Collision) 버그 수정:**
  - 적(Pawn)과 발사체(WorldDynamic) 간의 충돌 프로필 오류로 인해 총알이 멈추거나, 바닥을 뚫고 추락하는 물리 버그 해결.
  - 적과는 `Overlap`(통과), 바닥/벽과는 `Block`(막힘)으로 명확히 역할을 분리하고 `OnHit` 이벤트를 복구하여 가장 안정적인 충돌 판정 구현.

### 2026-05-20 (Mutation Synergy & Milestone System)
- **변이형 시너지 (Option A) 아키텍처:**
  - 주무기 발사체 하나에 모든 강화 효과가 중첩되는 시스템 구현.
  - `bLaserEnabled`, `bFlamethrowerEnabled` 등의 플래그를 통해 발사체의 형태(속도, 크기, 확산도 등)가 변이되면서도 관통/폭발 효과를 그대로 상속받음.
- **5레벨 마일스톤 레벨업 로직:**
  - `Level % 5 == 0`: `bIsWeapon == true`인 새로운 무기(변이 또는 패시브)만 등장.
  - 일반 레벨: 순수 스탯 증가 또는 *이미 보유한 무기의 후속 강화 카드*만 등장하도록 필터링 로직 고도화.
- **투사체 동적 변이 구현:**
  - `AEggProjectile`: `SetSpeed`, `SetActorScale3D` 등을 통해 발사 시점에 실시간으로 형태와 물리 특성을 변경 가능하도록 개선.
  - `UDuckCombatComponent`: 활성화된 모든 변이 플래그를 조합하여 최종 투사체의 파라미터를 계산하는 시너지 허브 역할 수행.
- **데이터 구조 개편:**
  - `EUpgradeType`을 `Stat`, `Weapon_Mod`(주무기 변이), `Weapon_Passive`(독립 무기)로 세분화하여 체계적인 데이터 관리 가능.
- **데이터 기반 업그레이드 아키텍처 구축:**
  - `UUpgradeDataAsset`: 이름, 설명, 아이콘, 타입, 강화 수치, 최대 레벨, VFX/SFX, 특수 액터 클래스를 포함하는 데이터 에셋 구현.
  - `EUpgradeType`: 스탯 강화(체력, 속도, 공격력 등), 메커니즘 강화(다중 발사, 관통, 폭발), 특수 기술(Orbit, Mine) 구분.
- **전투 시스템 고도화 (Mechanism Upgrade):**
  - **다중 발사 (Multi-Shot):** `MultiShotCount`에 따라 부채꼴 모양(Fan Pattern)으로 여러 발의 달걀을 동시에 발사하는 로직 구현.
  - **관통 (Piercing):** `bIsPiercing` 플래그 활성화 시 적을 뚫고 지나가며, `HitActors` 목록을 통해 중복 데미지를 방지하는 로직 구현.
  - **동적 데미지:** 캐릭터의 `BaseDamage` 스탯이 발사체에 실시간으로 전달되도록 수정.
- **레벨업 및 흐름 제어:**
  - `LevelUp()`: 경험치 가득 찰 시 게임 일시 정지(`SetGamePaused`), 마우스 활성화, 입력 모드 전환 및 랜덤 3종 업그레이드 추출 로직 구현.
  - `OnShowUpgradeScreen`: 블루프린트 UI에 데이터를 넘겨주기 위한 C++ 이벤트 정의.
  - `SelectUpgrade()`: 선택된 데이터를 적용하고 게임을 재개하며, 트윈스틱 슈팅을 위한 입력 모드(`GameAndUI`) 및 마우스 포커스 복구 로직 구현.
  - `ApplyUpgrade()`: 선택된 데이터에 따라 실제 캐릭터 스탯 및 컴포넌트 상태를 업데이트하고 나이아가라(Niagara) VFX 재생.
- **C++ 기반 UI 시스템 구축:**
  - `UUpgradeScreenWidget`: `BindWidget`을 사용하여 블루프린트의 텍스트, 이미지, 버튼을 C++과 직접 연결하고 데이터 바인딩 및 클릭 로직을 코드로 처리.
- **프로젝트 설정 업데이트:**
  - `EggThiefDuck.Build.cs`: 시각 효과 및 UI 포커스 처리를 위해 `Niagara`, `Slate`, `SlateCore` 모듈 의존성 추가.

### 2026-05-19 (Bug Fix)
- **사망 상태 유지 시스템 개선:**
  - **애니메이션 고정 로직:** 사망 애니메이션(Montage) 종료 시점에 `bPauseAnims`를 활성화하여 캐릭터가 Idle 상태로 돌아가지 않고 죽은 포즈를 유지하도록 수정.
  - **충돌 처리 최적화:** 사망 즉시 캡슐 컴포넌트의 충돌을 비활성화(`NoCollision`)하여 플레이어나 적이 사망한 시체에 걸리지 않도록 개선.
  - **타이머 기반 제어:** `DeathTimerHandle`을 사용하여 몽타주 재생 시간에 맞춰 정확하게 포즈를 고정하도록 구현.

### 2026-05-19 (Pivot & Hybrid)
- **게임 설계 변경:** 골드/상점 시스템을 삭제하고 '뱀파이어 서바이벌' 스타일의 실시간 레벨업/강화 시스템으로 전환 결정.
- **하이브리드 시스템:** 레벨업을 위한 XP 시스템과 별도로, 메타 성장을 위한 골드 아이템 시스템을 다시 추가.
- **문서 업데이트:** `GAME_DESIGN.md` 및 `history.md`에 새로운 성장 루프 및 재화 시스템 반영.

### 2026-05-15 (현재)
- **애니메이션 및 시각 효과 고도화:**
  - **애니메이션 시스템 (C++ & BP):** `UDuckAnimInstance` 구현. 속도, 방향(Direction), 사격 상태를 연동하여 8방향 이동 및 뒷걸음질 애니메이션 완벽 지원.
  - **비율 기반 공격 동기화:** `FireRate`에 따라 공격 애니메이션 속도를 동적으로 계산(`AnimLength / FireRate`).
  - **정밀 발사 타이밍:** 애니메이션의 진행률 50% 시점에 정확히 발사체가 생성되도록 동기화 로직 구현.
  - **상태 기반 몽타주:** 피격(`TakeDamage`) 및 사망(`Die`) 시 전용 몽타주가 재생되도록 C++ 트리거 연결.
- **전투 및 조작감 고도화:**
  - **부드러운 조준:** `RInterpTo`를 사용하여 사격 시 마우스 방향으로 빠르고 부드럽게 회전하도록 개선.
  - **사격 정확도 보정:** 캐릭터 정면이 조준 방향과 일정 각도(10도) 이내로 일치할 때만 발사되도록 제한.
  - **최소 1발 보장:** 짧은 클릭 시에도 조준 완료 후 반드시 1발을 발사하도록 `bWantsToFire` 플래그 도입.
- **아이템 시스템 물리 안정화:**
  - `ADropItemBase`: `InitVelocity` 호출 시 콜리전 상태(QueryAndPhysics)와 물리 시뮬레이션을 강제 활성화하여 튕겨나가는 연출 보장 및 에러 해결.
- **메인 HUD 및 플레이어 체력 시스템:**
  - `ADuckCharacter`: `MaxHealth`, `CurrentHealth` 추가 및 `TakeDamage` 시스템 구현.
  - `HealthBarWidget`: 플레이어 및 적 머리 위 공통 C++ 베이스 위젯 클래스 생성 및 연동.
  - `UMainHUDWidget`: 메인 화면 UI 전용 C++ 클래스 생성 (체력, 골드, 시간 업데이트 이벤트 제공).
  - `ADuckPlayerController`: 전용 컨트롤러를 통해 HUD 생성 및 데이터 브릿지 역할 수행.
  - `ADuckGameMode`: 시간 데이터를 HUD에 실시간 전달하도록 수정.
- **낮과 밤 사이클 및 환경 시스템:**
  - `ADuckGameMode`: 24시간 시계 및 게임 페이즈(Night, Morning, Day) 시스템 구현.
  - **정밀한 태양 궤적:** 쿼터니언 Slerp 보간을 이용해 6시(일출), 12시(정오), 18시(일몰) 수치를 정확히 통과하며 비스듬하게 회전하는 물리 기반 태양 궤도 구현.
  - **조명 최적화:** 6시 일출 시점 조명 밝기 동기화 및 에디터 수치 튐 현상 방지.
- **적 AI 행동 고도화 (Phase Interaction):**
  - **Morning Phase 대응:** 아침 6시가 되면 모든 적이 4초간 정지 후 플레이어 반대 방향으로 도망치는 로직 구현.
  - **시선 처리 개선:** 추격 시에는 플레이어를, 도망 시에는 도망 방향을 자연스럽게 바라보도록 `RInterpTo` 기반 회전 로직 적용.
- **아이템 시스템 고도화 및 클래스 분리:**
  - `AGoldItem`, `AAmmoItem` 구현 및 플레이어 스탯(Gold, Ammo Gauge) 연동.
  - `ADropItemBase` 물리 보강: 소환 시 강제로 `Simulate Physics`를 활성화하여 튕겨나가는 연출 보장.
- **빌드 오류 수정:**
  - `DuckCharacter`의 `Gold` 변수 접근 제한자 및 메타 태그(`AllowPrivateAccess`) 오류 수정.
  - `GoldItem`에서 `private` 멤버 직접 접근 대신 `Getter`를 사용하도록 수정.
- **데미지 및 UI 시스템 구현:**
  - `EnemyBase.cpp` 및 `DuckCharacter.cpp`에서 `Public/` 접두사가 포함된 잘못된 헤더 포함 경로 수정 (`C1083` 오류 해결).
- **데미지 및 UI 시스템 구현:**
  - `AEnemyBase` 체력 시스템: `TakeDamage` 오버라이드 및 `CurrentHealth/MaxHealth` 관리.
  - `Die()` 로직 구현: 체력 0 이하 시 액터 파괴 및 아이템 드롭 준비.
  - `HealthBarWidget`: 머리 위 `WidgetComponent` 추가 및 카메라를 향하는 Screen Space UI 설정.
  - `AEggProjectile` 데미지 연동: 명중 시 물리적 넉백과 함께 실제 데미지(20.0) 전달.
- **물리 기반 적 AI 및 상호작용 고도화:**
  - `APawn` 기반 물리 폰 리팩토링: `ACharacter`에서 전환하여 `Simulate Physics` 루트 이동 구현.
  - `BoxComponent` 도입: 캡슐 대신 박스를 루트로 사용하여 단단한 물리 상호작용 및 겹침 방지.
  - **연속 충돌 감지(CCD):** 발사체와 몬스터에 CCD를 활성화하여 고속 관통 현상 해결.
  - **지면 감지(Grounded Check):** LineTrace를 통해 바닥에 닿았을 때만 점프하도록 개선(공중 부유 해결).
  - **물리 안정화:** 넉백 시 기존 속도 상쇄 및 최대 속도 제한(Clamping) 적용.
  - **독립적 스케일 설정:** `SetAbsolute(Scale=true)`를 통해 메시 크기 변화 없이 충돌 영역만 조절 가능하도록 구현.
- **적 AI 기초 구현:**
  - `AEnemyBase` 클래스 생성 및 AI 추격 로직 구현.
  - `EggThiefDuck.Build.cs`: `AIModule`, `NavigationSystem`, `UMG` 모듈 의존성 추가.
- **전투 시스템 기초 구현:**
  - `AEggProjectile` 클래스 구현: 발사체 이동 및 충돌 로직.
  - `UDuckCombatComponent` 클래스 구현: 달걀 게이지(탄창), 자동 회복, 과열(Overheat) 로직.
  - `ADuckCharacter` 통합: 전투 컴포넌트 부착 및 마우스 좌클릭(FireAction) 입력 바인딩 완료.
- **기본 캐릭터 시스템 구현:**
  - `ADuckCharacter` 클래스 생성.
  - 탑뷰 카메라 시스템 (SpringArm + Camera) 설정.
  - Enhanced Input을 이용한 8방향 이동 구현.
  - 마우스 커서 위치를 추적하는 실시간 회전(LookAt) 로직 구현.
- **입력 에셋 설정:**
  - `IA_Move`, `IMC_Duck` 생성 및 적용.
- **프로젝트 초기화:**
  - `GEMINI.md`, `GAME_DESIGN.md`, `history.md` 작성 및 동기화.
